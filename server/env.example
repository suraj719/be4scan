# Database
# For Aiven PostgreSQL, use the connection pooler URL (recommended):
# postgresql://user:password@pooler-host:port/database?sslmode=require
# Or use direct connection (Prisma handles pooling internally):
# postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# Server
PORT=3001
NODE_ENV=development

# Authentication
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

# Docker
# Linux/Mac: /var/run/docker.sock
# Windows: //./pipe/docker_engine
# Leave unset to auto-detect based on OS
# DOCKER_SOCKET_PATH=/var/run/docker.sock

# Scan Configuration
SCAN_TIMEOUT_MINUTES=30
ARTIFACTS_DIR=./artifacts
